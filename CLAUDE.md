# CLAUDE.md — MOVDI · ops
Reglas y contexto del proyecto para Claude Code. Léelas antes de hacer cambios.
## Qué es este proyecto
Sistema interno de operaciones de MOVDI (gestión de peticiones, tareas, equipo). Se está refactorizando de una SPA de un solo index.html (JS inline + @supabase/supabase-js@2 en Netlify) a Next.js (App Router) + TypeScript + Tailwind con Supabase como backend. La app vieja se conserva como referencia visual hasta terminar la migración.

- Supabase project ref: nxyhgbrretusqbgfodmo
- Tablas: personas, peticiones, recurrentes, todos, anuncios, anuncios_vistos, notificaciones, recompensas, historial_mensual, estrellas_colaboracion
- Auth: Supabase Auth real (email + contraseña, reset de contraseña, panel RH con verificación adicional).
## Reglas de trabajo con Git
- Nunca trabajar directamente en main. Cada cambio va en su rama de feature y se abre PR. (La rama histórica `refactor/nextjs-migration` YA NO existe: la migración a Next.js ya vive en main.)
- No mergear a main sin revisión humana. Con OK explícito de dirección (revisando el PR) el merge a main sí procede.
- Antes de cada migración de base de datos o cambio de seguridad, mostrar el SQL/diff y esperar OK.
## Reglas de seguridad (no negociables)
- La service_role key JAMÁS lleva el prefijo NEXT_PUBLIC_, ni se importa en código cliente, ni se commitea. Solo en .env.local y en variables de servidor del hosting. Vive en lib/supabase/admin.ts (con `import 'server-only'`) y se consume en exactamente DOS lugares acotados, ambos server-only: (1) el invite de Auth en app/(app)/equipo/actions.ts → inviteUserByEmail (alta de personas Y la action `reenviarInvitacion` agregada 2026-07-20 con OK — mismo archivo, mismo propósito) y (2) el helper único de inserción de notificaciones (lib/supabase/notificar.ts). Cualquier uso nuevo de service_role requiere OK explícito y debe quedar documentado aquí.
- .env.local está en .gitignore. Solo se commitea .env.example con valores vacíos.
- RLS es la única barrera real. La anon key es pública; toda autorización se hace con políticas RLS basadas en auth.uid() y el nivel/área de la persona, no con lógica solo del cliente.
- El login NO muestra la lista de personas. Se pide email + contraseña. Cualquier listado de personas requiere sesión autenticada; un usuario anónimo no debe recibir ningún nombre ni email.
- Panel RH: NO hay contraseña extra (decisión 2026-07-03). El acceso RH se controla por nivel='rh' en personas + políticas RLS. Los datos de RH nunca deben depender de lógica solo-cliente.
- Peticiones privadas: visibles solo para creador y destinatario, ni siquiera dirección.
## Hallazgo crítico — RESUELTO (2026-07-03)
La tabla personas era legible de forma anónima (política RLS de SELECT abierta a anon exponía a las 21 personas con email, rol, nivel y área). Cerrado en dos pasos: (1) PR #1 a main eliminó el grid de login que dependía de esa lectura (login por email+contraseña), (2) migración cerrar_personas_select_anon aplicada: drop policy personas_select_anon + revoke select from anon. Verificado: petición anónima → permission denied; usuarios autenticados siguen leyendo. También resueltos: tablas ventas_* huérfanas públicas (respaldadas en backups/ y eliminadas), policies always-true (notificaciones/historial/estrellas/recompensas) y search_path/EXECUTE de las funciones mi_*.
## Decisiones acordadas para fases siguientes
- Fase 4, módulo "agregar persona": la Server Action de alta debe, además de insertar en personas, invitar al usuario a Auth automáticamente (supabase.auth.admin.inviteUserByEmail con service_role, solo en servidor), para cerrar de raíz el hueco de "persona sin cuenta". Ya no debe existir el flujo manual de invitar desde el dashboard.
- Escrituras sensibles por Server Action (enfoque híbrido): INSERT de notificaciones (su policy interim `mi_nombre() is not null` se endurece cuando este INSERT pase a servidor), historial_mensual, estrellas_colaboracion (límite 2/semana validado en servidor; la RLS ya lo respalda), recompensas y administración de personas. Lecturas siguen client-side con anon key + RLS.
- Privacidad de gamificación (Fase 4.8, 2026-07-05): flag `personas.ve_gamificacion_completa` (true solo Dani/Emmanuel) = ve todo. Ejecutivo: solo lo suyo; head: progreso de su equipo (sin estrellas/recompensas); RH: lista de recompensas ganadas + marcar entregadas (`historial_mensual.recompensa_entregada`, UPDATE limitado a esa columna por grant). Estrellas: solo donde participo. Catálogo de recompensas oculto al equipo (se descubre al ganar); editor solo Dani (RLS respalda: escritura dirección). Podio = solo MES CERRADO vía RPC `podio_mes_cerrado`. Todo en la migración cutover 6 (`20260705190000_cutover_gamificacion_privacidad.sql`) ✅ APLICADA (2026-07-06); la UI la respeta.
- Visibilidad por equipos (Fase 4.12, 2026-07-05, hallazgo de Leonardo): `recurrentes_select=true` exponía patrones ajenos a ejecutivos. Modelo nuevo en AMBAS tablas (peticiones/recurrentes): cada quien lo suyo (creador o para); heads su equipo en ambas direcciones (helper `es_de_mi_equipo`, misma relación del semáforo); dirección (es_direccion) todo; privadas de peticiones intactas (solo creador/para, ni heads ni dirección). Migración cutover 8 (`20260705230000_cutover_visibilidad_equipos.sql`) ✅ APLICADA (2026-07-06). Nota: la rama de "heads su equipo" fue reemplazada por la de "jefa directa" el 2026-07-20 (ver más abajo) — ahora el candado es la relación es_de_mi_equipo, sin exigir nivel='head'.
- Fase compromisos (2026-07-14): auto-asignación de primera clase en peticiones ("✋ nuevo compromiso": solicitante = asignado, misma tabla/tablero). `privada` FORZADA a false en servidor (un compromiso privado sería invisible para el líder). Columna nueva `peticiones.origen` (talento|cliente|interno|propio) **sin default** — histórico NULL = sin clasificar, obligatoria solo en la UI de creación. `updated_at` revivida como "último movimiento" con trigger CONDICIONAL (solo estatus/descripcion/nota_entrega/link_entrega/fecha_entrega; ocultar/marcar visto/privada/fecha NO cuentan). Regla de 3 días hábiles sin movimiento → "atorada" (calculada en cliente, sin campo). Panel "qué está atorado" para heads/dirección (client-side, RLS intacta). Anti-farmeo: compromisos propios (creador=para u origen='propio', nunca instancias recurrentes) EXCLUIDOS de toda la gamificación (XP/cumplimiento/rachas/logros) en lib/gamificacion.ts. "Mis to-dos" intactos y privados. Nota de avance ("+ nota"): línea que se agrega a `descripcion` sin tocar estatus — cuenta como movimiento (salida legítima para tareas bloqueadas por terceros; cambiar `fecha` NO limpia el rojo, a propósito). Migración 9 (`20260714120000_cutover_compromisos_origen_movimiento.sql`) ✅ APLICADA el 2026-07-14 con OK explícito — ver sección F de docs/CUTOVER.md.
- Formularios dinámicos por área (2026-07-15): peticiones a digital/admi/legal exigen `tipo_peticion` + campos del tipo en `peticiones.detalle` (jsonb whitelisteado). Config ÚNICA en `lib/tipos-peticion.ts` (código, no tabla): campos con clase `bloqueante` (sin él no se crea — validado en cliente Y servidor) o `recomendado` (se marca si falta, calculado en vivo, no almacenado; cambiar la clase = 1 palabra — pendiente validación campo-por-campo con cada área, ver docs/CAMPOS-FORMULARIOS.md). SLA → `fecha` automática en días hábiles (factura +2, alta portales +3), read-only, recalculada en servidor. Catálogo INTERNO `clientes` (sin conexión a la herramienta administrativa): todos leen/autocompletan (snapshot editable en `detalle`), escribe SOLO área admi + dirección (RLS con `mi_tiene_area`; un RFC mal capturado no lo pisa cualquiera), baja lógica `activo=false` (DELETE solo dirección; FK protege histórico), carga inicial por CSV y gestión en `/clientes`. Botón "guardar cliente al catálogo" (solo admi) pasa datos capturados a mano al catálogo. Índice único de nombre con `unaccent_inmutable` (wrapper IMMUTABLE de 2 args — el de 1 arg es STABLE y el índice fallaría). Migración 10 (`20260715120000_cutover_formularios_clientes_tipo_detalle.sql`) ✅ APLICADA el 2026-07-15 con OK explícito. Ajuste 2026-07-15: uso CFDI = dropdown del catálogo SAT c_UsoCFDI en factura y /clientes (se guarda la CLAVE; frecuentes G03/G01/S01/CP01 arriba; capturas legadas se normalizan con `normalizarUsoCFDI`); Legal usa selector "tipo de persona" (física oculta cargo y doc de facultades del firmante) — la columna del catálogo sigue siendo `persona_moral` boolean.
- Vínculo auth ↔ persona (2026-07-15, bug Valeria/Alfredo): `personas.auth_user_id` alimenta `mi_nombre()` y por tanto TODA la RLS de escritura; el legado lo llenaba en el primer login pero la app Next no lo hacía en ningún lado → las altas post-switch morían con el error crudo de RLS en su primera petición (filas de Valeria y Alfredo reparadas a mano con OK). Fix en 3 capas: (1) el alta de equipo liga `auth_user_id` inmediatamente con el id que devuelve el invite (update con sesión de dirección — service_role sigue SOLO en invite+notificar), (2) autocuración en el layout protegido vía policy `personas_self_link` (solo fila propia por email del JWT y solo si el vínculo está vacío), (3) UX: banner "cuenta no terminada de configurar" + mensaje claro en getContexto de peticiones en vez del error de RLS. Alta correcta y verificación en docs/ONBOARDING.md.
- Jerarquía: Valeria head + "jefa directa" (2026-07-20, ✅ APLICADA con OK explícito): Valeria Tetlamatzi subió a `nivel='head'` (mantiene área digital; NO se creó área "diseño"). Concepto **"jefa directa"** = ejecutiva/rh con gente a cargo vía la relación de managers (`manager_principal`/`managers`, por NOMBRE de pila — el mismo formato que compara `es_de_mi_equipo`). REUTILIZA esa relación: sin tabla ni nivel nuevos. Una jefa obtiene poderes SOLO sobre sus supervisadas: ver su tablero/peticiones, crear/editar/pausar sus recurrentes (crear: solo modo `una` hacia una supervisada, validado en el server action `recurrentes/actions.ts`), ver semáforos y "qué está atorado", dar toques, y ver su progreso de gamificación como head (progreso sí; estrellas/recompensas no). NO hereda: editar personas, publicar anuncios, ni asignar por grupos (heads/ejecutivos/todos) o por área — eso sigue siendo `ceo|head` (y `rh` donde aplica). Helper único cliente `supervisadasDe()`/`tengoSupervisadas()` en `lib/peticiones.ts` (espejo de `es_de_mi_equipo`); separador `esCreadorRecurrentePrivilegiado` vs `puedeCrearRecurrentes` en `lib/recurrentes.ts` (hardcodes Salvador/Arylene CONSERVADOS). Migraciones `20260720120000` (datos: Valeria head, Brenda→Valeria, Fátima→Zazil con Montserrat preservada en apoyo, Jimena→Fernanda) y `20260720120100` (RLS jefa directa). RLS: `peticiones_select`, `recurrentes_select` y `recurrentes_update/delete` pasaron de `nivel='head'` a `es_de_mi_equipo` — esto además CIERRA el hoyo de que un head administrara recurrentes de equipos ajenos (ahora solo las de su gente vía `es_de_mi_equipo(para)`). Privadas y dirección intactas. Pendiente: al dar de alta a la nueva persona de diseño, su `manager_principal` debe ser Valeria. **Actualización 2026-07-20 (auditoría, verificado contra la BD viva)**: las TRES migraciones del día (`20260720120000`, `20260720120100` y la limpieza `20260720130000`) SÍ están aplicadas — aparecen en el registro de migraciones del proyecto y las 4 policies coinciden. La limpieza dejó `Jimena.managers=[]` como esperado, pero el estado vivo de dos personas DIVERGIÓ después por ediciones manuales/UI: `Fátima.manager_principal='Gloria'` con `managers=[Zazil,Montserrat]` (la migración la había dejado con principal Zazil) y `Antonio.managers=[Montserrat,Dani]` (Dani reapareció tras la limpieza que lo quitaba; inofensivo — dirección ve todo de todos modos — pero redundante). `Leonardo.managers=[Montserrat]` y `Demian.managers=[Fernanda]` intactos a propósito (supervisión de apoyo intencional).
- Pendiente manual (dashboard): activar leaked password protection en Authentication → Passwords. (Revisión 2026-07-20: el advisor de leaked passwords ya no aparece — probablemente ya activado; confirmar el toggle a ojo.)
- Auditoría técnica 2026-07-20: entregables en `auditoria/` (mapa, inventario funcional, backend, hallazgos). Netlify: el dominio principal sirve el build Next; el site legado (`movdi-ops-legado`) seguía vivo — apagarlo/protegerlo es el paso C8 pendiente.
- Cambios post-auditoría (2026-07-20, decisiones de dirección en sesión): (1) correo OBLIGATORIO en el alta de personas (todas las activas ya tenían email; cierra el hueco "persona sin cuenta"); (2) botón "✉ reenviar invitación" en editar persona (server action con inviteUserByEmail — antes el aviso prometía un reintento que no existía); (3) KPIs de /peticiones CLICABLES (aplican su filtro; filtro nuevo `entregadas`); (4) clic en una notificación de la campana NAVEGA a la petición (`/peticiones?pet=<id>`, fila resaltada) y la campana puede pedir permiso de avisos del navegador (Web Notifications, solo con pestaña en segundo plano — paridad con el SPA); (5) UI de recurrentes ALINEADA con la RLS jefa-directa: pausar/eliminar visibles para creador, dirección o jefa/head directa de la persona asignada; mover fechas sigue siendo del creador (o ceo/head); (6) mobile: semáforos visibles debajo de la lista en pantallas chicas, KPIs 2 columnas, botones de to-dos/campana visibles sin hover, grids de modales colapsan; (7) card "peticiones privadas 🔒" con total GLOBAL vía RPC `conteo_privadas_activas` (solo cuenta — el contenido de privadas sigue vedado a todos, dirección incluida) — migración `20260720150000` ⏳ SIN APLICAR; (8) loop público de feedback ABIERTO a todo el equipo vía RPC anónima `loop_publico()` (solo mes/categoría/mensaje/respuesta; no se abrió la tabla para no exponer autores) — migración `20260720150100` ⏳ SIN APLICAR. Columna `personas.needs_pass` confirmada muerta (y existe `activa` duplicada de `activo`): candidatas a limpieza futura, baja prioridad. Estatus `archivada` de peticiones = 100% legacy (el código lo respeta pero nunca lo asigna — se documenta como estado histórico, sin UI). **Verificado 2026-09-08: ya son 0 filas vivas** (las 325 heredadas ya no están); las migraciones `20260720150000` y `20260720150100`, marcadas arriba como ⏳ SIN APLICAR, **sí aparecen aplicadas** en el registro del proyecto.
- Estatus 'cancelada' + salida alterna en la baja de persona (2026-09-08, ✅ APLICADA con OK explícito): desactivar a alguien EXIGÍA reasignar todo lo activo; cuando esas tareas ya no sirven, heredárselas a otra persona le ensucia carga y cumplimiento, y sin salida la baja se atoraba. Ahora el paso de reasignación ofrece, POR BLOQUE, "reasignar a X" (default, sin cambios) o cerrar en bloque: peticiones → estatus `cancelada`, recurrentes → `activa=false` (mismo estado que ⏸). Se pueden mezclar. **`cancelada` es un terminal NUEVO** (no se reusó `archivada`: es legacy del SPA con 0 filas vivas, mezclarlos haría inauditable el dato): la fila NO se borra, se le anexa una línea de rastro a `descripcion` (quién y por qué), `fecha_entrega` queda en null, y es un cierre NEUTRO — ni suma como entregada ni pesa como pendiente/vencida. **Cuidado al tocar conteos**: el criterio viejo `estatus !== 'entregado'` contaba una cancelada como pendiente; se sustituyó por los helpers ÚNICOS de `lib/peticiones.ts` (`estaAbierta`/`estaCerrada`/`cierreNeutro`) en TODOS los consumidores — KPIs y filtros de /peticiones, semáforo (`lib/equipo.ts`), "qué está atorado", privadas, progreso, RH, instancias de recurrentes y gamificación (fuera del numerador Y del denominador: no rompe rachas ni cuenta como vencida). Nueva UI: chip de filtro `canceladas` en /peticiones (su única entrada — a propósito NO es un KPI) y botón "reabrir" para creador/destinatario por si se canceló de más. Intactos: la reasignación existente, el check de rol ceo|head DENTRO de la RPC, la atomicidad todo-o-nada y toda la RLS. Migración `20260908120000_cutover_peticion_cancelada.sql` (amplía el CHECK de `peticiones_estatus_check` y hace DROP+CREATE de `desactivar_persona_con_reasignacion` con 2 params nuevos `default false` — la llamada vieja de 3 args sigue funcionando).
- Aprobación de entrega (2026-09-18, ✅ APLICADA con OK explícito): entregar no avisaba NADA a quien pidió la petición — el ciclo se cerraba de un solo lado. Ahora: (1) al entregar, el creador recibe notificación `entrega_por_aprobar` (📦, con la nota de entrega recortada a 80 chars) y la petición entra a su cola; (2) el creador **aprueba** (`aprobarEntrega` → sella `aprobada_en`/`aprobada_por`, notifica `entrega_aprobada` ✅) o **pide cambios** (`pedirCambios` → estatus de vuelta a `pendiente`, limpia `aprobada_*`, anexa la línea `↩ cambios pedidos (fecha, quién): motivo` a `descripcion` igual que las notas de avance, notifica `cambios_pedidos` ↩; motivo obligatorio 3–200). "Pedir cambios" REEMPLAZA al reabrir pelón del creador; el destinatario conserva su botón "reabrir". **No requieren aprobación** (nacen aprobadas de facto, CALCULADO en `lib/peticiones.ts`, sin escribir BD): instancias recurrentes y auto-asignadas/compromisos propios (creador = destinatario) — helpers ÚNICOS `requiereAprobacion`/`esperaAprobacion`/`estaAprobada`. UI: badge "⏳ por aprobar" + botones para el creador, "· esperando aprobación" para el destinatario, "· aprobada" pegado al label de `labelFecha` (el "Nd tarde" se conserva), KPI/filtro `por_aprobar` que solo aparece con N>0 y lleva a "lo que pedí", y el clic en la notificación abre la petición en la tab correcta (antes caía en "general" cuando yo era la creadora). **La gamificación NO se toca**: XP, cumplimiento, rachas, leaderboard y cierre de mes siguen leyendo `estatus='entregado'` + `fecha_entrega` como hoy; aprobar tampoco cuenta como movimiento (las columnas nuevas quedan fuera de la lista del trigger `peticiones_touch_movimiento`, así que no des-atora nada). Migración `20260918120000_cutover_aprobacion_entrega.sql` ✅ APLICADA el 2026-09-18 (registrada como `20260918200044`; las 7 verificaciones del pie del archivo corrieron limpias contra la BD viva): columnas `aprobada_en`/`aprobada_por` + backfill (las 949 entregadas vivas nacen aprobadas, si no la cola amanecería con 742 históricos) + trigger `peticiones_guard_aprobacion` ASIMÉTRICO: PONER el sello (valor no nulo) exige ser el creador — sin eso el destinatario se auto-aprobaría por API saltándose la Server Action —, pero LIMPIARLO (null) lo puede hacer cualquiera que ya edite la fila. **Re-entrega (2026-09-18, hueco detectado en revisión)**: una entrega aprobada conserva su botón `reabrir` y reabrir no limpiaba el sello, así que la re-entrega nacía aprobada, sin botones y sin cola aunque nadie la hubiera revisado; `entregarPeticion` ahora escribe `aprobada_en/aprobada_por = null` en CADA entrega (con reintento sin esas columnas si la migración aún no está aplicada, para que marcar entregado nunca dependa del orden migración/deploy). NO toca ninguna policy: los grants de `peticiones` son a nivel tabla, las columnas nuevas quedan escribibles sin tocar grants.
- Buscador de /peticiones (2026-09-18, sin migración): input arriba de las tabs que MANDA sobre la vista — mientras haya texto se busca en TODAS las peticiones que la RLS ya dejó cargar (`puedoVerPeticion`), ignorando pestaña, chip de filtro y el ocultar/mostrar ocultas, porque lo que se busca se quiere encontrar aunque esté entregado, oculto o cancelado. Campos: nombre, descripción, para, creado_por, nota_entrega, link_entrega y el cliente (del catálogo por `cliente_id` o el capturado a mano en `detalle.cliente_nombre`). Helper ÚNICO `coincideBusqueda` en `lib/peticiones.ts`: normaliza AMBOS lados con `normalizarTexto` (NFD, sin acentos ni mayúsculas — "disenar" encuentra "diseñar") y exige que TODAS las palabras aparezcan, en cualquier campo y en cualquier orden. Todo client-side sobre lo ya cargado (la carga de peticiones usa `selectTodo`, así que el histórico completo está disponible y no se topa en 1000). Debounce de 200ms; ✕ (o Escape) limpia; los KPIs también limpian la búsqueda al aplicar su filtro. Mientras se busca se apagan semáforo, panel de atorados, card de privadas y los controles de ocultas; el orden de resultados es por fecha DESCENDENTE (lo reciente arriba), distinto del tablero normal.
- Menú de tipos de Digital por PERSONA (2026-09-18, sin migración): el select de tipo de petición de `digital` dejó de ser uno solo para toda el área — ahora depende de QUIÉN recibe. El catálogo se define UNA vez en `TIPOS_POR_AREA.digital` (keys estables) y aparte vive `MENU_POR_PERSONA` (mapa área → persona → keys, por NOMBRE de pila, comparado con `matchNombre`). Vigentes (7, todos con el mismo campo bloqueante `link del brief (Notion)`, sin SLA ni cliente): `correo_incorporacion`, `media_kit`, `pieza_rrss`, `pitch_desarrollo_contenido`, `estrategia_identidad_marca`, `roster_web_actualizacion`, `desarrollo_identidad_marca`. Mapa: **Valeria** = correo/media kit/pieza RRSS/pitch-desarrollo/estrategia identidad; **Diana** (Diana Lopez, la activa — Diana Becerra está inactiva) = correo/media kit/pieza RRSS/roster-web/desarrollo identidad; **Brenda** (Brenda Reyes = "Bren") = correo/pieza RRSS. **Fallback**: quien NO esté en el mapa (alguien nuevo, Alfredo, o dirección) ve los 7 — entrar o salir del equipo nunca rompe el formulario. Grupo (modo área): se ofrece solo la INTERSECCIÓN de los destinatarios. El tipo se LIMPIA si cambias de destinatario y ya no aplica. Helpers ÚNICOS `tiposVigentesDeArea`/`tiposParaDestinatarios`/`tipoPermitidoPara`; `crearPeticion` valida con el mismo candado (por eso un tipo legacy o ajeno tampoco pasa por API). **Legacy**: los 10 tipos viejos (`pitch_deck`, `ideacion`, `asesoria_notion`, `roster_web`, `pieza_rrss_talento`…) quedan marcados `legacy: true` — fuera de todo menú, pero definidos para que el histórico conserve `etiquetaTipo` y su `detalle`; NO se migraron datos y por eso `roster_web` (legacy) y `roster_web_actualizacion` (vigente) conviven a propósito. Nota: digital ya no tiene tipos "sin brief" (la cobertura de `requiereDescripcion` vive ahora en la consulta administrativa de admi). Tabla por persona en docs/CAMPOS-FORMULARIOS.md.
- Paginación obligatoria (2026-08-06): PostgREST corta CADA respuesta en 1000 filas (max-rows default) y `peticiones` ya rebasa ese tope — un `select('*')` sin paginar trunca EN SILENCIO (bug: "lo que pedí" perdía peticiones nuevas; el cierre de julio se archivó con datos incompletos). TODA lectura completa de `peticiones` y `estrellas_colaboracion` (y cualquier tabla que crezca) usa el helper `lib/supabase/select-todo.ts`, que pagina en bloques de 1000 con orden + desempate único por `id`. El mock de e2e simula el tope (test de regresión en peticiones.spec.ts). `notificaciones` (~700 filas) no tiene lecturas sin tope (la campana usa `.limit(30)`), pero si se agrega una lectura completa, va con el helper.
- Recurrentes 4.2b (quincenal real con fecha_inicio + recordatorio diario por pg_cron): ✅ las 3 migraciones cutover_* APLICADAS (2026-07-06) y el cron `recordatorio-recurrentes-diario` PROGRAMADO y activo (schedule `0 13 * * *` UTC → `notificar_recurrentes_del_dia()`). Checklist histórica en docs/CUTOVER.md.
- **`recurrentes_avisos` = tabla interna CERRADA A PROPÓSITO (revisado 2026-09-08 — no la toques).** Los security advisors la marcan como `rls_enabled_no_policy` (INFO): **es esperado, NO le agregues policies.** No es una tabla de datos sino el libro de rayas del cron: PK `(recurrente_id, fecha)` para que `notificar_recurrentes_del_dia()` no avise dos veces lo mismo el mismo día (`insert ... on conflict do nothing` + `if found`). Ni siquiera esa función le hace SELECT, y hay CERO referencias en `app/`, `lib/` y `e2e/`. RLS sin policies + `revoke all from anon, authenticated` está escrito a mano en la migración `20260703230500` con su comentario: para una tabla que solo toca una función SECURITY DEFINER, eso es MÁS cerrado que tener policies — agregárselas sería una regresión (abriría a `authenticated` algo que nadie necesita). Los avisos que el equipo SÍ ve salen de `notificaciones` (tipo `recurrente_hoy`), que tiene sus policies y se lee normal desde la campana. Verificado vivo el 2026-09-08: 203 filas (2026-07-06 → hoy, 32 en los últimos 7 días, las 31 recurrentes existentes), cron con 64 corridas todas `succeeded`, y 32 notificaciones `recurrente_hoy` en la misma ventana — cadena completa 1:1. **Tampoco es basura que se pueda borrar**: sin ella el cron duplicaría avisos en cada re-run.
## Variables de entorno
NEXT_PUBLIC_SUPABASE_URL=          # público
NEXT_PUBLIC_SUPABASE_ANON_KEY=     # público (publishable)
SUPABASE_SERVICE_ROLE_KEY=         # SECRETO — solo servidor
## Arquitectura destino
app/(auth)/login/        · login por email, sin listar personas
app/(app)/...            · rutas protegidas por middleware
app/api/...              · Route Handlers para acciones sensibles
lib/supabase/client.ts   · createBrowserClient (anon)
lib/supabase/server.ts   · createServerClient (cookies)
lib/supabase/admin.ts    · service_role — SOLO server
proxy.ts                 · protege rutas y refresca sesión (Next 16 renombró middleware→proxy; la lógica vive en lib/supabase/middleware.ts)
supabase/migrations/     · esquema y RLS versionados

Usar @supabase/ssr con cookies (no localStorage). Migrar la UI módulo por módulo conservando paridad de funciones con el index.html viejo.
## Flujo de verificación
Después de cambios de seguridad, correr los security advisors de Supabase y hacer una petición anónima (solo anon key, sin sesión) a cada tabla: debe devolver 0 filas o 401/403. Probar la app por rol (ejecutivo / head / dirección / RH) antes de dar por cerrado un módulo.
